# Cochlear Implant Signal Processing Demo

This project provides a demonstration of the complete signal processing chain used in cochlear implants:

```
Input Speech → Filterbank → Envelope → n-of-m Selection → Compression → Vocoder
```
